machine:
  timezone:
    America/Los_Angeles
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build --rm=false -t sudomesh/sudowrt .
test:
  override:
    - docker run sudomesh/sudowrt ar71xx :
        timeout: 10000
